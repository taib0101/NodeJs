* go to terminal
1. npm install nodemon
* go to package.json
1. /*
{
  "dependencies": {
    "body-parser": "^1.20.3",
    "readline": "^1.3.0"
  },
  "scripts": {
    "start": "env1=staging nodemon index",
    "env2": "env2=production nodemon index"
  }
}
*/

* go to unix terminal
* ~/ npm start
or
* ~/ npm run env2
or
* env1=staging env2=production nodemon|node index.js (you can give environment variable for  multiple time)
